import React from 'react'

const Home = () => {
  return (
    <div>
      <div>Home Component</div>
      <button onClick={() => console.log('click')}>click</button>
    </div>
  );
};

export default Home;
